server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/liyong201/springcloud/
          searchPaths: repository
          force-pull: true
          username: *****
          password: *****
eureka:
  client:
    serviceUrl:
      defaultZone: http://peer1:8761/eureka/

